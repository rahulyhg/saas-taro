/**
 * nodata.state 参数类型
 *
 * @export
 * @interface NodataState
 */
export interface NodataState {
    nodataImgSrc: string
}

/**
 * nodata.props 参数类型
 *
 * @export
 * @interface NodataProps
 */
export interface NodataProps {
    show: boolean,
    stylevalue?: string,
    imgStyle?: string,
    notice?: string
}
