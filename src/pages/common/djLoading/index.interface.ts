// import { finished } from "stream";

/**
 * loading.state 参数类型
 *
 * @export
 * @interface djLoadingState
 */
export interface djLoadingState {
    [key:string]: any
}

/**
 * loading.props 参数类型
 *
 * @export
 * @interface djLoadingProps
 */
export interface djLoadingProps {
    isshow: boolean
}
