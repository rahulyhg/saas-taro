import {Request} from '../utils/request'

//  接口说明
export const ***** = (param) => {
    return Request.creatRequests({
        url: '/api/***/****',
        method: 'POST',
        data: param
    })
}